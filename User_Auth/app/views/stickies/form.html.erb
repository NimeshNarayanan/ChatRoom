<%= form_for(:sticky, :url => {:controller => 'stickies', :action => 'form'}) do |f| %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>


    <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit :create%>
  </div>
<% end %>
<%= link_to 'Back', stickies_index_path %>

